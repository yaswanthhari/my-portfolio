from setuptools import setup, find_packages

setup(
    name="my-portfolio",
    version="1.0.0",
    author="Yaswanth Hari",
    description="Python Developer Portfolio Projects",
    packages=find_packages(),
    install_requires=[
        # Dependencies listed in requirements.txt
    ],
)
